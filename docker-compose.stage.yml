version: '3'

services:
  web:
    restart: always
    build: 
      context: ./src
      args:
        gitbranch: master
    environment:
      - N_WORKERS=3
    env_file:
      - staging.env
    networks:
      - nginx_network
    image: web:v${VERSION_TAG}-stage
  
  nginx:
    restart: always
    build: 
      context: ./nginx
      args:
        nginxconf: douglasdaly.conf
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - web
    networks:
      - nginx_network
    image: nginx:v${VERSION_TAG}-stage

networks:
  nginx_network:
    driver: bridge
